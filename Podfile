# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'GCDWebServer_Embedded' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GCDWebServer_Embedded
pod "GCDWebServer", "~> 3.0"

  target 'GCDWebServer_EmbeddedTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GCDWebServer_EmbeddedUITests' do
    # Pods for testing
  end

end
